apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: myapp
    app.kubernetes.io/instance: myapp
  name: myapp
spec:
  ports:
  - port: 4200
    protocol: TCP
    targetPort: 4200
  selector:
    app: myapp
  type: ClusterIP
status:
  loadBalancer: {}


